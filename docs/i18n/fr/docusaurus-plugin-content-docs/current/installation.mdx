---
title: Installation
---

#

<div>
  <p align="center">
    <img src="/static/discord-ts.svg" width="564" />
  </p>
  <p align="center">
    <a href="https://discord.gg/yHQY9fexH9" class="badge">
      <img
        src="https://img.shields.io/discord/874802018361950248?color=5865F2&logo=discord&logoColor=white"
        alt="Discord server"
      />
    </a>
    <a href="https://www.npmjs.com/package/discordx" class="badge">
      <img
        src="https://img.shields.io/npm/v/discordx.svg?maxAge=3600"
        alt="NPM version"
      />
    </a>
    <a href="https://www.npmjs.com/package/discordx" class="badge">
      <img
        src="https://img.shields.io/npm/dt/discordx.svg?maxAge=3600"
        alt="NPM downloads"
      />
    </a>
    <a href="https://github.com/oceanroleplay/discord.ts/actions" class="badge">
      <img
        src="https://github.com/oceanroleplay/discord.ts/workflows/Build/badge.svg"
        alt="Build status"
      />
    </a>
    <a href="https://www.paypal.me/vijayxmeena" class="badge">
      <img
        src="https://img.shields.io/badge/donate-paypal-F96854.svg"
        alt="paypal"
      />
    </a>
  </p>
  <h1>
    <p align="center">discord.ts (discordx)</p>
  </h1>
  <p align="center">
    <b> Créez votre bot discord en utilisant TypeScript et decorators! </b>
  </p>
</div>

:::warning
Ce fichier a été traduit avec le traducteur google, s'il y a une erreur, nous nous excusons
:::

## Introduction

Ce module est une extension de **[discord.js](https://discord.js.org)**, donc le comportement interne (méthodes, propriétés, ...) est le même.

Cette bibliothèque vous permet d'utiliser des décorateurs TypeScript sur Discord.**js**, it simplify your code and improve the readability !

## Configuration facile - projet de démarrage

1. Cloner ce projet

```
git clone https://github.com/oceanroleplay/discord.ts-example
cd discord.ts-example
```

2. Construire

```bash npm2yarn
npm install
```

3. Jeton de bot de configuration

<Tabs
  defaultValue="win"
  values={[
    { label: "Windows", value: "win" },
    { label: "Linux", value: "linux" },
  ]}
>
<TabItem value="win">

```bash
set BOT_TOKEN=<your bot token>
```

</TabItem>

<TabItem value="linux">

```bash
export BOT_TOKEN=<your bot token>
```

</TabItem>
</Tabs>

4. Run

```bash npm2yarn
npm run start
```

5. Et tu as fini, tout a été fait pour toi! 🚀

## Installation

<Tabs
  defaultValue="stable"
  values={[
    { label: "Stable", value: "stable" },
    { label: "Development", value: "dev" },
  ]}
>
<TabItem value="stable">

Use [npm](https://www.npmjs.com/package/discordx) or [yarn](https://yarnpkg.com/package/discordx) to install **discordx** with **discord.js**

:::caution
Vous devez également installer "reflect-metadata" pour le decorators
:::

```bash npm2yarn
npm install discordx reflect-metadata discord.js
```

Installez également vos dépendances de développement TypeScript

```bash npm2yarn
npm install --save-dev @types/node typescript
```

Et vous devriez voir cela dans votre package.json

```json title="package.json"
{
  // ...
  "dependencies": {
    "discordx": "^5.X.X",
    "discord.js": "^13.X.X",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/node": "^16.X.X",
    "typescript": "^4.2.4"
  }
  // ...
}
```

Si vous avez besoin de mettre à jour à nouveau à l'avenir, exécutez simplement:

```bash npm2yarn
npm install discordx discord.js
```

</TabItem>

<TabItem value="dev">

Use [npm](https://www.npmjs.com/package/discordx) or [yarn](https://yarnpkg.com/package/discordx) to install **discordx** with **discord.js**

:::caution
Vous devez également installer "reflect-metadata" pour le decorators
:::

```bash npm2yarn
npm install discordx@dev discord.js@dev reflect-metadata
```

Installez également vos dépendances de développement TypeScript

```bash npm2yarn
npm install --save-dev @types/node typescript
```

Et vous devriez voir cela dans votre package.json

```json title="package.json"
{
  // ...
  "dependencies": {
    "discordx": "^5.X.X-dev.###",
    "discord.js": "^13.X.X-dev.###.###",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/node": "^16.X.X",
    "typescript": "^4.2.4"
  }
  // ...
}
```

Si vous avez besoin de mettre à jour à nouveau à l'avenir, exécutez simplement:

```bash npm2yarn
npm install discordx@dev discord.js@dev
```

</TabItem>
</Tabs>

## Execution environment

Pour démarrer votre bot, vous pouvez compiler votre code en JavaScript avec TypeScript en utilisant la commande `tsc` ou une simple utilisation [ts-node](https://www.npmjs.com/package/ts-node).

:::caution
Notez que si vous compilez votre code en JavaScript avec `tsc` vous devez spécifier le fichier .js lorsque vous instanciez votre client

```ts
const client = new Client({
  botId: "test",
  // prefix: "!",
  prefix: async (message: Message) => {
    return "!";
  },
  // glob string to load the classes
  classes: [
    `${__dirname}/commands/**/*.discord.{js,ts}`,
    `${__dirname}/events/**/*.discord.{js,ts}`,
  ],
  intents: [
    Intents.FLAGS.GUILDS,
    Intents.FLAGS.GUILD_MESSAGES,
    Intents.FLAGS.GUILD_MEMBERS,
  ],
  silent: false,
});

client.on("ready", () => {
  console.log(">> Bot started");

  // to create/update/delete discord application commands
  client.initApplicationCommands();
});

client.login(BOT_TOKEN);
```

:::

## tsconfig.json

Votre fichier tsconfig.json devrait ressembler à ceci:

```json title="tsconfig.json"
{
  "compilerOptions": {
    "module": "commonjs",
    "target": "ES2021",
    "noImplicitAny": true,
    "sourceMap": true,
    "strict": true,
    "outDir": "build",
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "declaration": false,
    "importHelpers": true,
    "forceConsistentCasingInFileNames": true,
    "lib": ["ES2021", "esnext.asynciterable"],
    "moduleResolution": "node"
  },
  "exclude": ["node_modules"]
}
```

## reflect-metadata

:::caution
Vous devez importer le module **reflect-metadata** dans votre fichier principal, pour réflexion.
Sinon, les décorateurs pourraient ne pas fonctionner correctement.
:::

```ts
import "reflect-metadata";
import { Client } from "discordx";

async function start() {
  // ...
}

start();
```

## Need help?

**[discordx server](https://discord.gg/yHQY9fexH9)**

**[discord.ts server](https://discord.gg/VDjwu8E)**

Vous pouvez également trouver de l'aide pour [examples folder](https://github.com/oceanroleplay/discord.ts/tree/main/examples)

## Voir également

- [Discord.js's documentation with Interactions (Slash commands)](https://discord.js.org/#/docs/main/master/general/welcome)
- [Discord's Slash commands interactions](https://discord.com/developers/docs/interactions/slash-commands)

## L'étape suivante

[Configurez et démarrez votre application 🚀](/docs/general/client)
